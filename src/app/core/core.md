(global stuff)

Core is for things used application-wide
Core should be imported once at bootstrap only.

Important:

    Imported only once in AppModule

    Never imported in feature modules

Core = infrastructure only.